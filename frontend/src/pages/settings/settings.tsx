import './settings.scss';

const Settings = () => {
  return (
    <div className="settings-container">
      <h1 className="settings-title">Настройки</h1>
      <p className="settings-text">Настрой параметры трекера.</p>
    </div>
  );
};

export default Settings;